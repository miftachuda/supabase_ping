name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 5 * * *" # once daily at 5 AM UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s "https://rgwjowrezjtspgyuyopz.supabase.co/rest/v1/loto?select=*" \
            -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}"
      - name: Ping Supabase 2
        run: |
          curl -s "${{ secrets.SUPABASE_URL_V2 }}/rest/v1/burner_states?select=*" \
            -H "apikey: ${{ secrets.SUPABASE_ANON_KEY_V2 }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY_V2 }}"
          - name: Ping Supabase 2
      - name: Ping Supabase 2
        run: |
          curl -s "${{ secrets.SUPABASE_URL_V3 }}/rest/v1/burner_states?select=*" \
            -H "apikey: ${{ secrets.SUPABASE_ANON_KEY_V3 }}" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY_V3 }}"

